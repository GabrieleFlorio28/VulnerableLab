services:
  sql-injection:
    build: ./sql-injection
    ports:
      - "5000:5000"
    volumes:
      - ./sql-injection:/app
    restart: unless-stopped

# altri scenari possono essere aggiunti come servizi separati
